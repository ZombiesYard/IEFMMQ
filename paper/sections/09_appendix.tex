% ===========================================================================
%  09 — Appendix
% ===========================================================================
\chapter{Appendix}\label{ch:appendix}

This appendix supports the methodology, implementation, and evaluation
chapters. It preserves raw configuration, dataset, replay, diagram, and
pilot-coding material without extending the claim boundaries stated in the
main chapters.

% ===========================================================================
\section{Full LoRA Hyperparameter Configuration}\label{sec:appendix-hyperparams}

Table~\ref{tab:lora-hyperparams-full} lists the complete hyperparameter
configuration for all three backbone models under the matched Run-003 +
Run-005$\times$2 recipe. Values are drawn from available training reports
and benchmark artifacts.

\begin{table}[htbp]
  \centering
  \caption{Complete LoRA fine-tuning hyperparameters across three backbones.}
  \label{tab:lora-hyperparams-full}
  \small
  \begin{tabular}{@{}llll@{}}
  \toprule
  \textbf{Parameter} & \textbf{Qwen3.5-9B} & \textbf{Qwen3.6-27B} & \textbf{Gemma-4-31B} \\
  \midrule
  \texttt{max\_seq\_length} & 4096 & 4096 & 4096 \\
  \texttt{num\_train\_epochs} & 4.0 & 4.0 & 4.0 \\
  \texttt{learning\_rate} & $2{\times}10^{-4}$ & $2{\times}10^{-4}$ & $2{\times}10^{-4}$ \\
  \texttt{per\_device\_train\_batch\_size} & 1 & 1 & 1 \\
  \texttt{gradient\_accumulation\_steps} & 4 & 4 & 4 \\
  \texttt{lora\_r} & 16 & 16 & 16 \\
  \texttt{lora\_alpha} & 16 & 16 & 16 \\
  \texttt{lora\_dropout} & 0.0 & 0.0 & 0.0 \\
  \texttt{seed} & 3407 & 3407 & 3407 \\
  \texttt{load\_in\_4bit} & True & True & True \\
  \texttt{warmup\_ratio} & 0.1 & 0.1 & 0.1 \\
  \texttt{weight\_decay} & 0.01 & 0.01 & 0.01 \\
  \texttt{train\_rows} & 928 & 928 & 928 \\
  \texttt{eval\_rows} & 0 & 0 & 0 \\
  \midrule
  \textbf{Backbone-specific} & & & \\
  \midrule
  Vision LoRA & enabled & enabled & disabled \\
  LoRA target modules & vision+lang & vision+lang & all-linear \\
  Chat template & Qwen default & Qwen default & gemma-4 \\
  GPU memory util. & 0.6 & 0.6 & 0.95 \\
  \midrule
  \textbf{Training outcomes} & & & \\
  \midrule
  Train runtime (s) & 6419 & 12619 & 8050 \\
  Train loss (final) & 0.2156 & 0.1068 & 0.0474 \\
  \bottomrule
  \end{tabular}
\end{table}

Note: training loss values are not directly comparable across backbones due to
differences in tokenizer behaviour, chat-template structure, target-sequence
distribution, and model parameterisation.

% ===========================================================================
\section{8-Fact V1 Baseline Benchmark}\label{sec:appendix-8fact}

The earliest complete benchmark uses an 8-fact LoRA adapter trained on Run-001
(180 tasks) and evaluated on the Run-002 holdout (50 samples). This line
predates the 13-fact ontology and is included for historical reference.

\begin{table}[htbp]
\centering
\caption{8-fact v1 benchmark: Qwen3.5-9B Base vs.\ LoRA-v1 on Run-002 holdout (50 samples, EN).}\label{tab:appendix-qwen-8fact}
\begin{tabular}{l c c c}
\toprule
\textbf{Metric} & \textbf{Base} & \textbf{LoRA-v1} & \textbf{$\Delta$} \\
\midrule
JSON valid rate       & 1.0    & 1.0    & --- \\
Schema valid rate     & 1.0    & 1.0    & --- \\
Fact accuracy         & 0.7600 & 0.9150 & +0.1550 \\
Macro $F_1$           & 0.4241 & 0.5847 & +0.1605 \\
Seen $F_1$            & 0.4714 & 0.8380 & +0.3666 \\
Sample exact match    & 0.06   & 0.36   & +0.30 \\
Critical false positives & 13  & 15     & +2 \\
\bottomrule
\end{tabular}
\end{table}

% Evidence: benchmarks/qwen35_vlm_finetune/base_vs_lora_holdout_run002_v1/

The LoRA adapter improves fact-level metrics substantially but increases
critical false positives from 13 to 15. This historical result illustrates the
8-fact v1 ontology's structural weakness: compound targets such as
\texttt{ins\_go} were not visually verifiable from single frames and produced
systematic false-positive errors on completion-type facts.

% ===========================================================================
\section{Dataset Fact Distributions}\label{sec:appendix-datasets}

Table~\ref{tab:fact-distributions} reports per-fact \texttt{seen} counts for
the two main training sources (Run-003, Run-005) and the two 13-fact holdout
sets (Run-002 newfacts, Run-004 random). Counts are drawn from the
corresponding \texttt{stats.json} files. The table uses short display labels;
the authoritative fact identifiers are defined in the visual-fact ontology
file used by the procedure pack.

\begin{table}[htbp]
\centering
\caption{Per-fact \texttt{seen} counts across training and holdout datasets (13-fact v2 ontology).}
\label{tab:fact-distributions}
\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xrrrr@{}}
\toprule
\textbf{Fact label} & \textbf{Run-003} & \textbf{Run-005} & \textbf{Run-002} & \textbf{Run-004} \\
\midrule
TAC page visible            & 46  & 24  & 23 & 3  \\
SUPT page visible           & 20  & 34  & 5  & 3  \\
FCS page visible            & 30  & 40  & 13 & 94 \\
FCS X marks visible         & 16  & 18  & 1  & 5  \\
BIT root page visible       & 18  & 43  & 9  & 16 \\
FCS-MC page visible         & 55  & 56  & 9  & 84 \\
FCS-MC intermediate result  & 18  & 15  & 1  & 7  \\
FCS-MC in-test state        & 18  & 19  & 6  & 17 \\
FCS-MC final GO result      & 18  & 22  & 2  & 60 \\
HSI page visible            & 106 & 109 & 49 & 100 \\
HSI map layer visible       & 79  & 37  & 27 & 50 \\
INS GRND alignment text     & 58  & 72  & 42 & 69 \\
INS OK text                 & 12  & 14  & 4  & 0  \\
\bottomrule
\end{tabularx}
\end{table}

% Evidence: datasets/vision_sft_run003/stats.json,
%           datasets/vision_sft_run005_composition_rebalance/stats.json,
%           datasets/vision_sft_holdout_run002_newfacts/stats.json,
%           datasets/vision_sft_holdout_run004_random/stats.json

Run-005 increases coverage for several sparse targets when combined with
Run-003: SUPT page observations rise from 20 to 54, FCS-MC page observations
from 55 to 111, and INS GRND alignment text observations from 58 to 130. This
table documents data provenance; Chapter~\ref{ch:evaluation} evaluates whether
the resulting training recipe improves model behaviour on holdout sets.

% ===========================================================================
\section{CLI Command Reference}\label{sec:appendix-cli}

The \texttt{simtutor} package provides the following CLI commands:

\begin{description}
\item[\texttt{run}.] Executes a simulation using a mock environment adapter
with a pre-recorded scenario file and writes the event log.
\item[\texttt{replay}.] Validates an existing event log against a procedure
pack, checking step ordering and state-machine consistency.
\item[\texttt{score}.] Scores an event log using the scoring engine, producing
omission counts, state-violation counts, and a total error score.
\item[\texttt{record-vlm}.] Runs VLM fact extraction on a set of vision
observations and records predictions for benchmarking.
\item[\texttt{replay-bios}.] Replays a raw DCS-BIOS capture through the
telemetry pipeline and optionally invokes the help generation loop.
\item[\texttt{validate}.] Validates JSONL files against a named schema from
the schema registry.
\end{description}

All commands accept model configuration overrides (\texttt{--model-provider},
\texttt{--model-name}, \texttt{--model-base-url}, \texttt{--model-timeout-s})
and a language switch (\texttt{--lang}).

% ===========================================================================
\section{Model Provider Configuration}\label{sec:appendix-model-config}

Model access is configured through environment variables and validated at
runtime:

\begin{description}
\item[Provider.] \texttt{SIMTUTOR\_MODEL\_PROVIDER} selects
\texttt{openai\_compat}, \texttt{ollama}, or \texttt{stub}.
\item[Base URL.] \texttt{SIMTUTOR\_MODEL\_BASE\_URL} sets the provider
endpoint.
\item[Model.] \texttt{SIMTUTOR\_MODEL\_NAME} selects the model name.
\item[Timeout.] \texttt{SIMTUTOR\_MODEL\_TIMEOUT\_S} sets the request timeout.
\item[Credentials.] \texttt{SIMTUTOR\_MODEL\_API\_KEY} is required for the
\texttt{openai\_compat} provider. API keys are redacted from log output.
\item[Images.] \texttt{SIMTUTOR\_MODEL\_ENABLE\_MULTIMODAL} toggles base64
image encoding in requests.
\item[Language.] \texttt{SIMTUTOR\_LANG} selects \texttt{zh} or \texttt{en}.
\end{description}

% ===========================================================================
\section{Replay Evaluation Suite}\label{sec:appendix-replay}

Table~\ref{tab:replay-cases} lists the five replay evaluation cases in the
\texttt{fa18c\_startup\_v04} suite. Display labels are shortened here; the
suite file preserves the exact \texttt{case\_id} strings.

\begin{table}[htbp]
\centering
\caption{Replay evaluation cases.}
\label{tab:replay-cases}
\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}Xccl@{}}
\toprule
\textbf{Case} & \textbf{Step} & \textbf{Vision} & \textbf{Target} \\
\midrule
No-op capture & S01 & no & battery switch \\
Battery on & S02 & no & fire-test switch \\
Battery on, engine generator off & S01 & no & left generator switch \\
FCS reset / FCS BIT & S18 & yes & FCS BIT switch \\
INS mode & S12 & yes & INS mode knob \\
\bottomrule
\end{tabularx}
\end{table}

% Evidence: replay_eval/fa18c_startup_v04/suite.yaml

Cases marked as vision available include per-frame sidecar files
(\texttt{frames.jsonl}) in the corresponding DCS World Saved Games capture.
These files allow vision-dependent replay cases to run without a live DCS World
instance.

% ===========================================================================
\section{Supplementary Architecture and Export Diagrams}\label{sec:appendix-figures}

The main chapters use simplified diagrams to support the research argument.
Figures~\ref{fig:harness-validation}, \ref{fig:deployment-topology}, and
\ref{fig:experiment-export} retain additional engineering detail for
reproducibility and operational context.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_harness_validation.pdf}
  \caption{Harness and evidence validation. Candidate tutor actions are checked
  against schema constraints, UI allowlists, evidence consistency, and procedure
  state before an overlay action plan is accepted; unsafe or under-grounded
  actions are repaired, rejected, or downgraded to text-only guidance.}
  \label{fig:harness-validation}
  \Description[Harness validation boundary]{A flow diagram shows an evidence
  packet and ranked step candidates feeding a structured model response. Schema
  and evidence-reference checks either accept a known UI target or send the
  response to repair, downgrade, or text-only fallback.}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_deployment_topology.pdf}
  \caption{Deployment topology used by the prototype. The simulator, Quest 3
  overlay path, local runtime, replay utilities, and remote model providers are
  separated so that live trials and offline analysis can use the same evidence
  and logging contracts.}
  \label{fig:deployment-topology}
  \Description[Prototype deployment topology]{A deployment diagram shows the
  Windows simulator host with DCS World, Quest 3, DCS-BIOS telemetry, and a
  vision sidecar connected to the SimTutor runtime. The runtime exchanges model
  calls with a remote GPU server and writes JSONL and CSV logs.}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/fig_experiment_export.pdf}
  \caption[Detailed experiment export pipeline]{Detailed experiment export pipeline. Runtime event logs and trial
  metadata are transformed into participant-level summaries, step coding,
  help-cycle records, quality gates, and study-level analysis tables.}
  \label{fig:experiment-export}
  \Description[Experiment export pipeline]{A pipeline diagram shows raw JSONL
  trial logs, session metadata, and pack taxonomy entering the experiment
  export step. The outputs are trial summary, step coding, help cycle, action
  timeline, and quality-gate files.}
\end{figure}

% ===========================================================================
\section{VR Pilot Study --- Supplementary Data}\label{sec:appendix-pilot}

Table~\ref{tab:pilot-participants} lists participant experience profiles used
for descriptive interpretation. P02's questionnaire-derived fields are excluded
from workload, confidence, usability, trust, and comment summaries because the
available questionnaire metadata contains an internal participant-id mismatch.
P08 uses manual completion coding, and P09 is treated as an independent
participant after the correction recorded in the trial metadata.

\begin{table}[htbp]
\centering
\caption{Pilot participant experience profiles (self-reported).}\label{tab:pilot-participants}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lllllll@{}}
\toprule
\textbf{P} & \textbf{Condition} & \textbf{Group} & \textbf{DCS World} & \textbf{F/A-18C} & \textbf{Cold start} & \textbf{VR} \\
\midrule
P01 & No tutor & novice & none & none & none & very little \\
P02 & No tutor & intermediate & frequent & experienced & tried before & frequent \\
P03 & No tutor & intermediate & occasional & tried before & tried before & occasional \\
P04 & Tutor & intermediate & occasional & tried before & tried before & occasional \\
P05 & Tutor & expert & frequent & experienced & can complete & frequent \\
P06 & No tutor & expert & frequent & experienced & can complete & frequent \\
P07 & No tutor & intermediate & occasional & tried before & tried before & occasional \\
P08 & Tutor & intermediate & occasional & tried before & tried before & occasional \\
P09 & Tutor & novice & none & none & none & very little \\
\bottomrule
\end{tabular}
}
\end{table}

Table~\ref{tab:pilot-uncompleted} lists the specific steps that were not
confirmed as completed by the available export evidence for each without-tutor
participant. It should be read together with the manual error table in
Chapter~\ref{ch:evaluation}, because passive without-tutor logging did not run
the VLM page checks used in tutor trials.

\begin{table}[htbp]
\centering
\caption{Steps not confirmed as completed in the without-tutor condition.}\label{tab:pilot-uncompleted}
\small
\begin{tabularx}{\textwidth}{@{}l>{\raggedright\arraybackslash}X@{}}
\toprule
\textbf{P} & \textbf{Steps not confirmed} \\
\midrule
P01 & S09, S12, S18, S19, S20, S22, S24, S27, S29, S31 \\
P02 & S02, S14, S18, S19, S27, S30 \\
P03 & S07, S09, S16, S18, S19, S26, S27, S29, S32 \\
P06 & S07, S18, S19, S27 \\
P07 & S02, S09, S12, S13, S16, S17, S18, S19, S22, S26, S27, S28, S29, S32 \\
\bottomrule
\end{tabularx}

S18 (FCS-MC BIT page on right DDI) and S19 (FCS BIT final GO result) remain
unconfirmed in all five without-tutor export rows and are vision-priority
display checks. This does not mean that all five participants omitted both
actions. Action-timeline review found right-DDI PB5 activity for all five
without-tutor participants, and FCS BIT switch-up activity for P02, P03, P06,
and P07. The remaining uncertainty is whether the page transition and final GO
result were visually present, because passive without-tutor export did not run
the VLM/page checks used in tutor trials. S27 (flap switch to AUTO) is also
unconfirmed by all five rows, but the procedure pack classifies it as
BIOS-priority with variable and gate evidence rather than VLM-bound visual
evidence. The table is therefore an export-confirmation table, not a direct
omission table.
\end{table}

Table~\ref{tab:pilot-errors-auto} reports the auto-coded error counts (before
manual review) alongside the manual error counts, showing how much the
automated export had to be corrected before pilot interpretation.

\begin{table}[htbp]
\centering
\caption{Auto-coded vs.\ manual error counts per participant.}\label{tab:pilot-errors-auto}
\small
\begin{tabular}{@{}llrrrrr@{}}
\toprule
\textbf{P} & \textbf{Cond.} & \textbf{Auto OM} & \textbf{Auto CO} & \textbf{Man. OM} & \textbf{Man. CO} & \textbf{Man. total} \\
\midrule
P01 & No tutor & 4  & 3  & 8  & 0 & 10 \\
P02 & No tutor & 0  & 3  & 0  & 0 & 1  \\
P03 & No tutor & 0  & 4  & 7  & 0 & 11 \\
P06 & No tutor & 0  & 3  & 2  & 0 & 4  \\
P07 & No tutor & 1  & 9  & 11 & 0 & 11 \\
P04 & Tutor    & 0  & 4  & 0  & 3 & 7  \\
P05 & Tutor    & 0  & 3  & 0  & 3 & 6  \\
P08 & Tutor    & 0  & 3  & 0  & 0 & 0  \\
P09 & Tutor    & 0  & 3  & 0  & 3 & 5  \\
\bottomrule
\end{tabular}

Auto-coded errors are from the automated pipeline (Auto\_Error\_* columns in
\texttt{step\_coding.csv}). Manual errors are from the human-reviewed columns
(Error\_*). P08's auto errors were all overridden to zero by manual review
(see data-quality note in Section~\ref{sec:eval-pilot}).
\end{table}

Table~\ref{tab:pilot-data-quality-authority} records the supplementary
artifact authority rules used when raw exports and manually reviewed evidence
diverge.

\begin{table}[htbp]
\centering
\caption{Supplementary pilot data-quality authority rules.}
\label{tab:pilot-data-quality-authority}
\small
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{0.10\textwidth}YYY@{}}
\toprule
\textbf{Case} & \textbf{Data-quality issue} & \textbf{Authority rule} & \textbf{Reported use} \\
\midrule
P08 & \texttt{trial\_summary.csv} reports incomplete progress, while reviewed
\texttt{step\_coding.csv} marks all 33 steps completed. & Manual telemetry
review and reviewed step-coding columns override the automatic trial summary. &
Reported as completed 33/33 with zero manual procedural errors. \\
P02 & Questionnaire JSON is stored under P02 but contains a P01 participant-ID
metadata mismatch. & Task logs remain usable; questionnaire-derived fields are
excluded. & Included in task and error tables; excluded from TLX, ratings, and
comment summaries. \\
P07 & Without-tutor questionnaire contains tutor-specific numeric fields even
though tutor-specific NA flags are true. & Condition and NA flags override raw
tutor-specific numeric fields. & General user rating retained; tutor rating
reported as N/A. \\
P04 & Quality-gate metadata contains non-blocking linkage warnings. & Retain
manual task metrics and help-cycle counts; do not overinterpret affected
linkage fields. & Included in trial overview with provenance caveat. \\
\bottomrule
\end{tabularx}
\end{table}
