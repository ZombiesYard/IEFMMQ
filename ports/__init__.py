"""Port interface package.

Ports define boundaries for external systems (environment, overlay, user
interaction, knowledge, models, event storage). Adapters will implement these
interfaces; domain core depends only on the abstractions defined here.
"""

